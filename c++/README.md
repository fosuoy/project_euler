Hopefully there should be nothing in here that does not use standard libraries.

To quickly run things here, try:
`TEMPLATE="problem_001" ; make ${TEMPLATE} ; ./${TEMPLATE} ; rm ${TEMPLATE}`

Assuming you're using bash
